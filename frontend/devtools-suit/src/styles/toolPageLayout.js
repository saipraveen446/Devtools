/** Shared shell for tool pages (matches ASCII / Base64 converter layout). */
export const toolPageOuter = {
  maxWidth: '800px',
  margin: '2rem auto',
  padding: '2rem',
  fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif',
  backgroundColor: '#f5f7fa',
  minHeight: '100vh',
};

export const toolPageTitle = {
  fontSize: '2rem',
  color: '#1a202c',
  marginBottom: '2rem',
  textAlign: 'center',
  fontWeight: '700',
};

export const toolPageCard = {
  backgroundColor: '#ffffff',
  borderRadius: '12px',
  boxShadow: '0 2px 8px rgba(0,0,0,0.08)',
  overflow: 'hidden',
  marginBottom: '2rem',
};
